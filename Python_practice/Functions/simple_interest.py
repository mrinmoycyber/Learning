#simple interest - si = P*T*R / 100
# P - principal amount
# T - time period
# R- Interest rate per year (%)

def simple_interest(p, t, r):
    return (p * t * r) / 100

print(simple_interest(p = 10, t = 5, r = 8))